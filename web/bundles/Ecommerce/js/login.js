var loginControllers = angular.module('login',[]);

loginControllers.controller('loginController', ['$scope', '$routeParams', '$http',
  function($scope, $routeParams, $http) {
     alert('dcvsdvd');
    }

  ]);